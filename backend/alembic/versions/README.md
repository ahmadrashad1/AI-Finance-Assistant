# Migrations

Empty for now — no ORM models exist yet (Milestone 1 is infrastructure only).
Migrations will be generated here via `alembic revision --autogenerate` once
the Finance domain introduces its first models.
